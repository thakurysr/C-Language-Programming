#include<stdio.h>
main()
{
    int i=4,j=2;
    switch(i)
    {
    case 1:
        printf("\nTo err is human,to forgive is against company policy");
        break;
    case j:
        printf("\nIf you have nothing to do, don't do it here");
    }
}
